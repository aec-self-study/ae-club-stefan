version: 2

models:
  - name: customers
    description: Cleaned up data for our customers
    columns:
      - name: customer_id
        description: Primary key of the model
        tests: 
          - unique
          - not_null
      - name: name 
        description: name of the customer
      - name: first_order_at
        description: date where the customer placed their first order